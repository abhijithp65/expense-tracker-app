name: expense_tracker
description: "A polished expense tracker with charts and clean architecture."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  equatable: ^2.0.5
  sqflite: ^2.3.3
  path: ^1.9.0
  intl: ^0.19.0
  uuid: ^4.4.0
  fl_chart: ^0.69.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
